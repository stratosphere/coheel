Reziprok
Triangle

different neighbourhood files

Testdatensatz

train pre classifierGA


language in config


redirects in main program
port of job manager in config


name und type
full hd

program parameter -p

dataSetConf renaming
cluster
port environment variables


schema package with to know which is which

context redirect resolving directly in main program


parse the neighbours file correctly
